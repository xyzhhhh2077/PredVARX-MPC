# copyAF_op9_and_p1pp

## Purpose

1. **Opinion 9**: Schur + discrete Lyapunov numerical probes; terminal cost ON/OFF short CL compare.  
   **Not** recursive feasibility / stability proofs.
2. **P1++**: multi-seed tight `y_max` (0.50 / 0.45) coverage for `Sigma_eps` modes and `Sigma_obs` modes.

Does not modify earlier copies.

## Run

```matlab
cd('.../experiments/copyAF_op9_and_p1pp');
addpath(pwd); addpath('lib');
run('copyAF_op9_and_p1pp.m');
```
